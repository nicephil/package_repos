[ "$ACTION" = "ifup" -o "$ACTION" = "ifupdate" ] && {
    [ "${DEVICE:0:6}" = "br-lan" ] && { 
        pid=`pgrep -f "arpwatch.*${DEVICE}\..*"`
        [ -n "$pid" ] && exit
        rm -rf /tmp/arpwatch/arp_${DEVICE}.dat
        touch  /tmp/arpwatch/arp_${DEVICE}.dat
        /usr/sbin/arpwatch -f /tmp/arpwatch/arp_${DEVICE}.dat -i $DEVICE
    }
}


