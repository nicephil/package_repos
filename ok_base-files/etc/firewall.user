# This file is interpreted as shell script.
# Put your custom iptables rules here, they will
# be executed with each firewall (re-)start.

# Internal uci firewall chains are flushed and recreated on reload, so
# put custom rules into the root chains e.g. INPUT or FORWARD or into the
# special user chains, e.g. input_wan_rule or postrouting_lan_rule.

iptables -t mangle -A FORWARD -o t_+ -p tcp -m tcp --tcp-flags SYN,RST SYN -j TCPMSS --set-mss 1452
iptables -t mangle -A FORWARD -i t_+ -p tcp -m tcp --tcp-flags SYN,RST SYN -j TCPMSS --set-mss 1452

iptables -t mangle -N statistic
iptables -t mangle -N statistic_tx
iptables -t mangle -N statistic_rx
iptables -t mangle -N statistic_tx_wan
iptables -t mangle -N statistic_rx_wan
iptables -t mangle -I FORWARD -j statistic
iptables -t mangle -A statistic -o eth0 -j statistic_tx_wan
iptables -t mangle -A statistic -o eth1 -j statistic_tx_wan
iptables -t mangle -A statistic -o eth2 -j statistic_tx_wan
iptables -t mangle -A statistic -i eth0 -j statistic_rx_wan
iptables -t mangle -A statistic -i eth1 -j statistic_rx_wan
iptables -t mangle -A statistic -i eth2 -j statistic_rx_wan
iptables -t mangle -A statistic -j statistic_tx
iptables -t mangle -A statistic -j statistic_rx

iptables -t nat -N SS_SPEC_WAN_AC
iptables -t nat -A SS_SPEC_WAN_AC -m set --match-set gfwlist dst -p tcp -m comment --comment "_SS_SPEC_WAN_AC_" -j REDIRECT --to-ports 1080 2>/dev/null
iptables -t nat -A OUTPUT -p tcp -m comment --comment "_SS_SPEC_RULE_" -j SS_SPEC_WAN_AC
iptables -t nat -A PREROUTING -p tcp -m comment --comment "_SS_SPEC_RULE_" -j SS_SPEC_WAN_AC
# iptables -t nat -A zone_TRUSTED_prerouting -p tcp -m comment --comment "_SS_SPEC_RULE_" -j SS_SPEC_WAN_AC

